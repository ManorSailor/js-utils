/**
 * Find maximum number among n numbers
 * @param {...number} nums
 * @returns {number}
 * @example Basic Example
 * ```js
 * max(1,2,3,4,5) // 5
 * ```
 */
function max(...nums) {
  return Math.max(...nums);
}

export { max as max5 };
